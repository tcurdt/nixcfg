# Save the output of this file and use kubectl create -f to import
# it into Kubernetes.
#
# Created with podman-4.7.0

# NOTE: The namespace sharing for a pod has been modified by the user and is not the same as the
# default settings for kubernetes. This can lead to unexpected behavior when running the generated
# kube yaml in a kubernetes cluster.
---
apiVersion: v1
kind: Pod
metadata:
  annotations:
    io.podman.annotations.infra.name: foo-infra
  creationTimestamp: "2023-10-25T21:05:21Z"
  labels:
    app: foo
  name: foo
spec:
  containers:
  - image: docker.io/ealen/echo-server:latest
    name: foo-server
    ports:
    - containerPort: 80
      hostPort: 8080

